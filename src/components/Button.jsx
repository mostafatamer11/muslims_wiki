import React from "react";
import { motion } from "framer-motion";

// const variants = {
//   primary: {
//     color:
//   }
// }

const Button = ({ variants }) => {
  return;
};

export default Button;
